[YAML]
# C105-antigravity 强制走美国节点（2026-08-22 新增）
# C105 (10.0.1.105) 为 Antigravity-Manager 反代，上游 Google Cloud Code 区域校验严格
# 默认 AI服务/谷歌服务组 fallback 可能选中 SG/UK 节点被 Google 拒绝，用源 IP 规则置顶强制走美国节点
# 注意：策略组名含 emoji，必须用引号包裹（Mihomo YAML 规则要求）
+rules:
  - "SRC-IP-CIDR,10.0.1.105/32,🇺🇸 美国节点"
